{
  "builds": [
    {
      "src": "server/api/**/*.js",
      "use": "@vercel/node"
    },
    {
      "src": "dist/**/*",
      "use": "@vercel/static-build"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server/api/$1" },
    { "src": "/(.*)", "dest": "/dist/$1" }
  ]
}
